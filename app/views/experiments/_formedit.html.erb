<div class=" viewport ">

<%= form_for(@experiment) do |f| %>
  <% if @experiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment.errors.count, "error") %> prohibited this experiment from being saved:</h2>

      <ul>
      <% @experiment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, rows: 3, :class=>"textbox small" %>
  </div>
  <br>
  <div class="field">
    <%= f.label :procedure %><br>
    <%= f.text_area :procedure, rows: 20, :class=>"textbox large"%><br> 
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url, cols: 60, :class=>"textbox small" %>
  </div>
  <div class="field">
	<%= f.collection_select(:project_id, Project.all, :id, :title) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

</div>